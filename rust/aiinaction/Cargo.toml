[package]
name = "aiinaction"
version = "0.1.0"
edition = "2021"
description = "Reusable, tested reference implementations accompanying the AI in Action book"
license = "MIT"

[lib]
path = "src/lib.rs"
